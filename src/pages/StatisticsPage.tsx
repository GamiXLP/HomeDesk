import { useEffect, useMemo, useState } from 'react';
import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from 'recharts';
import { Card } from '../components/ui/Card';
import { getTickets } from '../lib/tickets';
import type { Ticket } from '../types/database';
export function StatisticsPage() { const [tickets,setTickets]=useState<Ticket[]>([]); useEffect(()=>{getTickets().then(setTickets)},[]); const data=useMemo(()=>Object.entries(tickets.reduce<Record<string,number>>((acc,t)=>{acc[t.category]=(acc[t.category]||0)+1; return acc;},{})).map(([name,value])=>({name,value})),[tickets]); return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-3"><Stat label="Tickets gesamt" value={tickets.length}/><Stat label="Offen" value={tickets.filter(t=>!['done','archived','rejected'].includes(t.status)).length}/><Stat label="Erledigt" value={tickets.filter(t=>t.status==='done').length}/></div><Card className="p-6"><h2 className="mb-4 text-lg font-bold">Tickets nach Kategorie</h2><div className="h-80"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" nameKey="name" outerRadius={110} label>{data.map((_, i)=><Cell key={i}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></Card></div> }
function Stat({label,value}:{label:string; value:number}){return <Card className="p-5"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></Card>}
